version: '3'
networks:
  isolation-network:
    driver: bridge
services:
    sampleservice:
      build:
        context: ../../../tmp1
        dockerfile: sampleservice.docker
      ports:
        - "8888:8888"
      networks:
        - isolation-network
    sampleserviceappkey:
      build:
        context: ../../../tmp1
        dockerfile: sampleserviceappkey.docker
      ports:
        - "8889:8889"
      networks:
        - isolation-network
    sampleservicebasic:
      build:
        context: ../../../tmp1
        dockerfile: sampleservicebasic.docker
      ports:
        - "9998:9998"
      networks:
        - isolation-network
    sampleserviceacckey:
      build:
        context: ../../../tmp1
        dockerfile: sampleserviceacckey.docker
      ports:
        - "9999:9999"
      networks:
        - isolation-network
    taskserver:
      build:
        context: ../../../tmp1
        dockerfile: taskserver.docker
      ports:
        - "9600:9600"
      networks:
        - isolation-network
    keymgr:
      build:
        context: ../../../tmp1
        dockerfile: keymgr.docker
      ports:
        - "4003:4003"
      networks:
        - isolation-network
    workflow:
      build:
        context: ../../../tmp1
        dockerfile: workflow.docker
      ports:
        - "4004:4004"
      networks:
        - isolation-network
